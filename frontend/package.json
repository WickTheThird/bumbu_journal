{
  "name": "my-react-app--",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "prebuild:check": "npm run build && npm run test"
  },
  "devDependencies": {
    "vite": "^7.1.2",
    "vitest": "^4.0.18"
  }
}
